__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/
.pytest_cache/
.venv/
easy45_out/
/data/
bench/*.log
*.run.log
*.fai
*.mmi

# keep packaged reference data (anchor + CMs) — NOT ignored:
!src/easy45/data/
bench/ident/

# benchmark tool comparison intermediates (heavy) — keep only small result files
bench/getorganelle/
bench/ribotin/

# raw BLAST dumps (transient)
results_45S/its_blast*.txt
